```bash
cd [project-name]          
uv venv                             # Create a .venv
source .venv/bin/activate           # Get into .venv
uv pip install -r requirements.txt  # Install package depends on requirements.txt
uv run python main.py               # Run script
```
